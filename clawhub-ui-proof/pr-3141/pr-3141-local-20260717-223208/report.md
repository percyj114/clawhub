# PR #3141 UI proof

Captured from a real local ClawHub instance at `http://127.0.0.1:10472` in Chromium with a 1280 × 900 viewport.

- Skill fixture: `devSeed:seedPadelSkill`, opened at `/local/skills/padel?published=true`.
- Plugin fixture: a real ZIP upload named `ui-proof-muted-plugin-v2`, published through `/plugins/publish` as the local admin persona.
- Verified in both dark and light themes that the skill and plugin success-card icons use the muted marketplace icon treatment.
