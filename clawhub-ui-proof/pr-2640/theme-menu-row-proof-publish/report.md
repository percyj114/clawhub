# Compact theme menu proof

Verified on a real signed-in local-auth ClawHub instance:

- theme controls render as a three-button row below Settings
- a separator appears above the theme row and above Sign out
- selecting Light and Dark updates the stored theme mode
