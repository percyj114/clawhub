# Header Typeahead Creator Metadata Proof

Scenario: local ClawHub preview at `http://localhost:3003/` using the shared public-read Convex backend.

Steps:
1. Opened the home page in Chromium at 1366x768.
2. Focused the header global search input.
3. Searched for `expedia`.
4. Verified the creator row text is `Expedia Group@expediagroup`.
5. Verified `/ Org` is absent and the official badge is still present.

Evidence:
- Screenshot: `candidate/screenshots/01-header-typeahead-expedia.png`
